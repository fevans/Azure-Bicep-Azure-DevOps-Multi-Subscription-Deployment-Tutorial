# Bicep compiled output — generated by 'az bicep build' and should not be committed.
# The pipeline compiles Bicep on the fly; committing .json files creates stale
# ARM templates that could diverge from the source .bicep files.
bicep/**/*.json
bicep/*.json
